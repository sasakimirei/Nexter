{
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.89.1",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "watch:sass": "sass sass/main.scss css/style.css -w",
    "devserver": "live-server",
    "start": "concurrently \"pnpm watch:sass\" \"pnpm devserver\""
  }
}
